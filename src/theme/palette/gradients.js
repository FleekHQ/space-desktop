import palette from './palette';
/* eslint-disable max-len */

const fleekRainbow = `linear-gradient(to right, ${palette.fleekPink} 6%, ${palette.fleekTeal} 37%, ${palette.fleekTurquoise} 67%, ${palette.fleekYellow} 92%)`;

const gradients = { fleekRainbow };

export default gradients;
